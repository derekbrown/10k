import React from 'react';
interface ProviderProps {
    children: React.ReactNode;
    network?: number;
    rpcUrl?: string;
}
export declare const Provider: ({ network, children, rpcUrl }: ProviderProps) => JSX.Element;
export {};
