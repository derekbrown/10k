import type { Contract } from "@ethersproject/contracts";
import type { BaseProvider, JsonRpcProvider } from "@ethersproject/providers";
import type { Fetcher } from "../../types";
export declare type EthersContract = typeof Contract;
export declare type EthersFetcherConfig = {
    ethers?: {
        Contract: EthersContract;
    };
    provider: BaseProvider | JsonRpcProvider;
};
export declare type EthersFetcherConfigEthersLoaded = EthersFetcherConfig & {
    ethers: {
        Contract: EthersContract;
    };
};
export declare type EthersFetcher = Fetcher<EthersFetcherConfig>;
