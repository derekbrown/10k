import type { Address, FetchContext, NftMetadata } from "../../types";
export declare function cryptoKittiesMetadata(id: string, { jsonProxy }: FetchContext): Promise<NftMetadata>;
export declare function isCryptoKitties(contractAddress: Address): boolean;
