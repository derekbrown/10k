import type { Address, ContractMethod, NftMetadata } from "../../types";
export declare function cryptoPunksMetadata(index: string, cryptoPunksImage: (tokenId: string, method: ContractMethod) => Promise<string>): Promise<NftMetadata>;
export declare function isCryptoPunks(contractAddress: Address): boolean;
