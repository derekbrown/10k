import type { Address, NftMetadata } from "../../types";
export declare function decentralandEstateMetadata(tokenId: string): Promise<NftMetadata>;
export declare function isDecentralandEstate(contractAddress: Address): boolean;
