import type { Address, NftMetadata } from "../../types";
export declare function decentralandParcelMetadata(tokenId: string): Promise<NftMetadata>;
export declare function isDecentralandParcel(contractAddress: Address): boolean;
